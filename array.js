var friendsAge =[11, 21, 45, 1, 714, 105, 6];

// to decalre the position of array is- the first one will be '0' and then 1,2,3..
// position 0, 1, 2, 3.. ta k bole-- array lenght bole and individually 1,2,3 k index bole 



var picnicFee = [5000, 2000, 3000, 4000, ]

var nayikas = [ 'mahi', "opu", 'sabnor', 'sabana' ]

var vowels = ['a', 'e', 'i', 'o', 'u']

console.log(friendsAge.length);

// there are two conditions for the array-
// 1. if you write Number. only number, no mix up 
// 2. if it is only string then it will be only string
